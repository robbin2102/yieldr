{
  "name": "yieldr-rpc-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal server-side proxy that forwards a fixed allowlist of read-only JSON-RPC calls to the real RPC provider (e.g. Alchemy), so the provider API key never reaches the browser.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
